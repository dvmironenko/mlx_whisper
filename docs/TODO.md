# Список задач проекта MLX-Whisper

## Выполненные задачи

### 2026-05-07

| Задача | Статус | Описание |
|--------|--------|----------|
| Перевод статусов на русский | ✅ | Статусы заданий отображаются на русском: «В очереди», «Обработка», «Готово», «Ошибка», «Отменён» |
| Сортировка заданий по дате (новые первыми) | ✅ | GET /jobs возвращает список, отсортированный по `created_at` descending |

### 2026-05-05 / 2026-05-06

| Задача | Статус | Описание |
|--------|--------|----------|
| Асинхронная очередь транскрипции | ✅ | `TranscriptionQueueManager` на `ThreadPoolExecutor` + `Queue` (3 воркера, max 20 заданий) |
| Конфигурация очереди | ✅ | `TRANSCRIBER_WORKERS`, `QUEUE_MAX_SIZE` в `.env`, `JobStatus` enum, `JobManager` singleton |
| Сервисный слой транскрипции | ✅ | `TranscriptionService` — обёртка над `TranscriptionQueueManager` + `JobManager` |
| POST /transcribe async | ✅ | Возвращает `job_id` сразу, обработка в фоне |
| POST /transcribe-url async | ✅ | Загрузка видео по URL и транскрипция в фоне |
| GET /jobs — список заданий | ✅ | Возвращает metadata всех заданий |
| GET /jobs/{job_id} — детали | ✅ | Возвращает metadata + текст + сегменты результата |
| DELETE /jobs/{job_id} — отмена | ✅ | Отменяет queued/processing задания через queue, удаляет файлы |
| Перезапуск TranscriptionQueueManager | ✅ | Старт при запуске приложения, shutdown при остановке |
| Переписанные job cards | ✅ | Статус-бейджи, polling обновлений, модальное окно результата |
| Redirect после submit | ✅ | Перенаправление на список заданий вместо inline-отображения |
| Fix: редирект на '/' | ✅ | Исправлен редирект с `'index.html'` на `'/'` |
| Fix: конфликт pytest модулей | ✅ | Переименован `test_root_endpoint.py` для корректного импорта |
| Сохранение результатов в файлы | ✅ | Текст и segments JSON сохраняются в `data/uploads/{job_id}/` |
| Sync Playwright тесты | ✅ | Тесты `test_job_card_files.py` и `test_job_deletion.py` для async queue |

### 2026-04-29 / 2026-04-30

| Задача | Статус | Описание |
|--------|--------|----------|
| Генерация отчётов через LLM | ✅ | Endpoint `POST /api/v1/report/{job_id}`, модуль `src/models/report.py`, переменные OPENAI_API_KEY, OPENAI_MODEL, OPENAI_REPORT_PROMPT |
| Фонная генерация отчётов | ✅ | Генерация в `ThreadPoolExecutor`, `generate_report_via_openai_sync()` |
| RecursiveCharacterTextSplitter | ✅ | Текст разбивается на части до 10000 символов, отчёт генерируется для каждой части |
| Удаление разделителя | ✅ | Убран `"\n\n---\n\n"` из конкатенации чанков |
| Кеширование моделей Whisper | ✅ | Модели кэшируются в памяти между транскрипциями |
| Загрузка видео по URL | ✅ | Поддержка YouTube, Vimeo, прямых ссылок (`ALLOWED_URL_DOMAINS` в `.env`) |
| Копировать в модалке файлов | ✅ | Кнопка копирования текста в модальном окне просмотра |

### 2026-04-27

| Задача | Статус | Описание |
|--------|--------|----------|
| Модальное окно подтверждения | ✅ | Удаление заданий через модальное окно вместо `confirm()` |
| Модальное окно уведомления | ✅ | Сообщения об успехе/ошибке через модальные окна вместо `alert()` |
| Валидация размера файла до сохранения | ✅ | Проверка через `Content-Length` header до сохранения файла |

### 2026-04-26

| Задача | Статус | Описание |
|--------|--------|----------|
| Временные метки по умолчанию | ✅ | `DEFAULT_WORD_TIMESTAMPS=true` в `.env`, чекбокс отмечен |
| Кнопка «Просмотр» для текстовых файлов | ✅ | Открытие текста транскрипции в модальном окне |
| Кнопки скачивания и удаления | ✅ | Для каждого файла в списке заданий |
| Полный Job ID | ✅ | Отображение полного UUID в карточке задания |
| Кнопка удаления задания | ✅ | Удаление целого задания через модалку подтверждения |
| Сохранение файлов с метками | ✅ | Файлы с `word_timestamps=true` сохраняются с временными метками |
| Исправление параметров silenceremove | ✅ | Исправлены параметры FFmpeg фильтра для корректной фильтрации тишины |
| Исправление даты создания | ✅ | Исправлена отображаемая дата в карточках заданий |

### 2026-04-24

| Задача | Статус | Описание |
|--------|--------|----------|
| Сохранение результатов | ✅ | Все файлы сохраняются в `data/uploads/{job_id}/` |
| API для файлов | ✅ | Роуты `GET /jobs`, `GET /files/{filename}/download` |

### 2026-04-23

| Задача | Статус | Описание |
|--------|--------|----------|
| Перемещение переключателя темы | ✅ | `.theme-switch` перемещён из header в правый верхний угол страницы (fixed positioning) |

### 2026-02-22 / 2026-02-28

| Задача | Статус | Описание |
|--------|--------|----------|
| Реализация транскрипции с MLX-Whisper | ✅ | Основная функция транскрипции аудио в текст через MLX-Whisper |
| Минималистичный интерфейс | ✅ | Новая UI-страница с формой транскрипции и расширенными параметрами |
| Клиентская валидация файлов | ✅ | Проверка типа и размера файла на клиенте (`validateFile`) |
| Поддержка MKV | ✅ | Добавлена поддержка загрузки и обработки MKV файлов |
| Копировать результат | ✅ | Кнопка копирования текста транскрипции |
| Initial prompt | ✅ | Параметр `initial_prompt` для контекстной транскрипции |
| Кнопка «Копировать» в результате | ✅ | Копирование распознанного текста в буфер обмена |
| Оптимизация кнопок | ✅ | Выравнивание, горизонтальное отображение, единый стиль |
| Обработка NaN и Infinity | ✅ | JSON-сериализация с очисткой NaN/Infinity |
| Система логирования | ✅ | RotatingFileHandler, app.log + error.log, дублирование в stdout |
| Измерение времени | ✅ | Замер времени конвертации и транскрипции, логирование результатов |
| Переименование параметров | ✅ | Убран префикс `DEFAULT_` из названий параметров |
| Рефакторинг CLAUDE.md | ✅ | Удалены дублирующие разделы, оставлены уникальные с ссылками на документацию |
| Создан TODO.md | ✅ | Список задач проекта |

---

### 2026-05-08 / 2026-05-11

| Задача | Статус | Описание |
|--------|--------|----------|
| VibeVoiceEngine интеграция | ✅ | `VibeVoiceEngine` как механизм транскрипции через oMLX API, параметр `mechanism=vibevoice` |
| Автоматическое разбиение аудио | ✅ | Разбиение по тишине (librosa), группировка интервалов, лимит 50 мин / 100 MB |
| Определение спикеров VibeVoice | ✅ | 0 → "Клиент", 1 → "Терапевт", автоматический fallback на текстовый парсинг |
| Типы отчётов | ✅ | Endpoint `GET /api/v1/report-types`, конфигурация в `src/services/report_types.py` |
| Кэш моделей — preload/clear/stats | ✅ | Endpoints `POST /cache/preload`, `POST /cache/clear`, `GET /cache/models` |
| Сохранение raw ответа API | ✅ | `{base_name}_raw.json` для Whisper и VibeVoice, endpoint `GET /files/{filename}/content` |
| Размер файла в карточках заданий | ✅ | `size` поле в метаданных, отображение в UI |
| Удаление отдельных файлов | ✅ | `DELETE /jobs/{job_id}/files/{filename}`, `GET /jobs/{job_id}/files/{filename}/download` |
| Единый формат результата | ✅ | `{segments, text, speaker_detected, transcription_duration, raw_response}` для всех механизмов |

---

## План задач

### Приоритет высокий

- [ ] Добавить тесты с реальными аудиофайлами
- [ ] Оптимизировать скорость транскрипции для больших файлов

### Приоритет средний

- [ ] Добавить индикатор прогресса при загрузке и обработке
- [ ] Добавить поддержку пакетной обработки файлов
- [ ] Улучшить мобильную версию веб-интерфейса
- [ ] Глобальный поиск по сохранённым транскрипциям

### Приоритет низкий

- [ ] Добавить горячие клавиши для основных действий
- [ ] Реализовать экспорт результатов в PDF
- [ ] Поддержка SRT формата для субтитров

---

## Архитектурные улучшения

- [ ] Контейнеризация через Docker
- [ ] Миграция на async/await для всех операций ввода-вывода
