# Список задач проекта MLX-Whisper

## Выполненные задачи

### 2026-04-15

| Задача | Статус | Описание |
|--------|--------|----------|
| Рефакторинг CLAUDE.md | ✅ | Удалены дублирующие разделы, оставлены только уникальные разделы с ссылками на другую документацию |
| Перемещение переключателя темы | ✅ | `.theme-switch` перемещен из header в правый верхний угол страницы (fixed positioning) |
| Изменение цвета переключателя | ✅ | Цвет фона переключателя в светлом режиме изменён на серый (`#a0aec0`), кнопка - черный, текст - темно-серый |
| Системные тесты API | ✅ | Есть 10+ тестовых файлов в `tests/` (test.py, test_endpoint.py, test_transcribe_*.py) |
| Валидация расширений | ✅ | Есть на клиенте (`validateFile`) и сервере (`validate_file_extension`) |

### 2026-04-24

| Задача | Статус | Описание |
|--------|--------|----------|
| Сохранение результатов | ✅ | Все файлы сохраняются в `data/uploads/{job_id}/` |
| API для файлов | ✅ | Роуты `GET /jobs`, `GET /files/{filename}/download` |

### 2026-04-26

| Задача | Статус | Описание |
|--------|--------|----------|
| Временные метки по умолчанию | ✅ | `DEFAULT_WORD_TIMESTAMPS=true` в .env, чекбокс отмечен |
| Модальное окно подтверждения | ✅ | Удаление заданий через модальное окно вместо `confirm()` |
| Модальное окно уведомления | ✅ | Сообщения об успехе/ошибке через модальные окна вместо `alert()` |
| Удаление файлов через модалку | ✅ | Удаление конкретного файла с подтверждением |

---

## План задач

### Приоритет высокий

- [ ] Включить валидацию размера файла (`validate_file_size`) в router.py
- [ ] Добавить тесты с реальными аудиофайлами
- [ ] Оптимизировать скорость транскрипции для больших файлов

### Приоритет средний

- [ ] Добавить индикатор прогресса при загрузке и обработке
- [ ] Реализовать кэширование моделей в памяти
- [ ] Добавить поддержку пакетной обработки файлов
- [ ] Улучшить мобильную версию веб-интерфейса

### Приоритет низкий

- [ ] Добавить горячие клавиши для основных действий
- [ ] Реализовать экспорт результатов в PDF
- [ ] Поддержка SRT формата для субтитров
- [ ] Глобальный поиск по сохраненным транскрипциям

---

## Архитектурные улучшения

- [ ] Вынести конфигурацию в отдельный модуль
- [ ] Добавить поддержку аутентификации через API ключ
- [ ] Реализовать систему очередей для обработки задач
- [ ] Контейнеризация через Docker
- [ ] Миграция на async/await для всех операций ввода-вывода
