# Список задач проекта MLX-Whisper

## Выполнено

- [x] Перевод статусов на русский — статусы заданий отображаются на русском: «В очереди», «Обработка», «Готово», «Ошибка», «Отменён»
- [x] Сортировка заданий по дате (новые первыми) — GET /jobs возвращает список, отсортированный по `created_at` descending
- [x] Асинхронная очередь транскрипции — `TranscriptionQueueManager` на `ThreadPoolExecutor` + `Queue` (3 воркера, max 20 заданий)
- [x] Конфигурация очереди — `TRANSCRIBER_WORKERS`, `QUEUE_MAX_SIZE` в `.env`, `JobStatus` enum, `JobManager` singleton
- [x] Сервисный слой транскрипции — `TranscriptionService` — обёртка над `TranscriptionQueueManager` + `JobManager`
- [x] POST /transcribe async — возвращает `job_id` сразу, обработка в фоне
- [x] POST /transcribe-url async — загрузка видео по URL и транскрипция в фоне
- [x] GET /jobs — список заданий — возвращает metadata всех заданий
- [x] GET /jobs/{job_id} — детали — возвращает metadata + текст + сегменты результата
- [x] DELETE /jobs/{job_id} — отмена — отменяет queued/processing задания через queue, удаляет файлы
- [x] Перезапуск TranscriptionQueueManager — старт при запуске приложения, shutdown при остановке
- [x] Переписанные job cards — статус-бейджи, polling обновлений, модальное окно результата
- [x] Redirect после submit — перенаправление на список заданий вместо inline-отображения
- [x] Fix: редирект на '/' — исправлен редирект с `'index.html'` на `'/'`
- [x] Fix: конфликт pytest модулей — переименован `test_root_endpoint.py` для корректного импорта
- [x] Сохранение результатов в файлы — текст и segments JSON сохраняются в `data/uploads/{job_id}/`
- [x] Sync Playwright тесты — тесты `test_job_card_files.py` и `test_job_deletion.py` для async queue
- [x] Генерация отчётов через LLM — endpoint `POST /api/v1/report/{job_id}`, модуль `src/models/report.py`, переменные OPENAI_API_KEY, OPENAI_MODEL, OPENAI_REPORT_PROMPT
- [x] Фонная генерация отчётов — генерация в `ThreadPoolExecutor`, `generate_report_via_openai_sync()`
- [x] RecursiveCharacterTextSplitter — текст разбивается на части до 10000 символов, отчёт генерируется для каждой части
- [x] Удаление разделителя — убран `"\n\n---\n\n"` из конкатенации чанков
- [x] Кеширование моделей Whisper — модели кэшируются в памяти между транскрипциями
- [x] Загрузка видео по URL — поддержка YouTube, Vimeo, прямых ссылок (`ALLOWED_URL_DOMAINS` в `.env`)
- [x] Копировать в модалке файлов — кнопка копирования текста в модальном окне просмотра
- [x] Модальное окно подтверждения — удаление заданий через модальное окно вместо `confirm()`
- [x] Модальное окно уведомления — сообщения об успехе/ошибке через модальные окна вместо `alert()`
- [x] Валидация размера файла до сохранения — проверка через `Content-Length` header до сохранения файла
- [x] Временные метки по умолчанию — `DEFAULT_WORD_TIMESTAMPS=true` в `.env`, чекбокс отмечен
- [x] Кнопка «Просмотр» для текстовых файлов — открытие текста транскрипции в модальном окне
- [x] Кнопки скачивания и удаления — для каждого файла в списке заданий
- [x] Полный Job ID — отображение полного UUID в карточке задания
- [x] Кнопка удаления задания — удаление целого задания через модалку подтверждения
- [x] Сохранение файлов с метками — файлы с `word_timestamps=true` сохраняются с временными метками
- [x] Исправление параметров silenceremove — исправлены параметры FFmpeg фильтра для корректной фильтрации тишины
- [x] Исправление даты создания — исправлена отображаемая дата в карточках заданий
- [x] Сохранение результатов — все файлы сохраняются в `data/uploads/{job_id}/`
- [x] API для файлов — роуты `GET /jobs`, `GET /files/{filename}/download`
- [x] Перемещение переключателя темы — `.theme-switch` перемещён из header в правый верхний угол страницы (fixed positioning)
- [x] Реализация транскрипции с MLX-Whisper — основная функция транскрипции аудио в текст через MLX-Whisper
- [x] Минималистичный интерфейс — новая UI-страница с формой транскрипции и расширенными параметрами
- [x] Клиентская валидация файлов — проверка типа и размера файла на клиенте (`validateFile`)
- [x] Поддержка MKV — добавлена поддержка загрузки и обработки MKV файлов
- [x] Копировать результат — кнопка копирования текста транскрипции
- [x] Initial prompt — параметр `initial_prompt` для контекстной транскрипции
- [x] Кнопка «Копировать» в результате — копирование распознанного текста в буфер обмена
- [x] Оптимизация кнопок — выравнивание, горизонтальное отображение, единый стиль
- [x] Обработка NaN и Infinity — JSON-сериализация с очисткой NaN/Infinity
- [x] Система логирования — RotatingFileHandler, app.log + error.log, дублирование в stdout
- [x] Измерение времени — замер времени конвертации и транскрипции, логирование результатов
- [x] Переименование параметров — убран префикс `DEFAULT_` из названий параметров
- [x] Рефакторинг CLAUDE.md — удалены дублирующие разделы, оставлены уникальные с ссылками на документацию
- [x] Создан TODO.md — список задач проекта
- [x] VibeVoiceEngine интеграция — `VibeVoiceEngine` как механизм транскрипции через oMLX API, параметр `mechanism=vibevoice`
- [x] Автоматическое разбиение аудио — разбиение по тишине (librosa), группировка интервалов, лимит 50 мин / 100 MB
- [x] Определение спикеров VibeVoice — ID спикеров из ответа oMLX, автоматический fallback на текстовый парсинг
- [x] Типы отчётов — endpoint `GET /api/v1/report-types`, конфигурация в `src/services/report_types.py`
- [x] Кэш моделей — preload/clear/stats — endpoints `POST /cache/preload`, `POST /cache/clear`, `GET /cache/models`
- [x] Сохранение raw ответа API — `{base_name}_raw.json` для Whisper и VibeVoice, endpoint `GET /files/{filename}/content`
- [x] Размер файла в карточках заданий — `size` поле в метаданных, отображение в UI
- [x] Удаление отдельных файлов — `DELETE /jobs/{job_id}/files/{filename}`, `GET /jobs/{job_id}/files/{filename}/download`
- [x] Единый формат результата — `{segments, text, speaker_detected, transcription_duration, raw_response}` для всех механизмов
- [x] Формат текстовых файлов — Whisper: `[MM:SS]: Текст`, VibeVoice: `[MM:00] Спикер N : Текст`
- [x] VibeVoiceEngine — ID спикеров из API — убран маппинг 0→"Клиент", 1→"Терапевт"; используются ID из ответа oMLX
- [x] Единый формат текста `_build_formatted_text_from_segments` — общая функция форматирования для Whisper и VibeVoice
- [x] Исправление чтения segments для VibeVoice — `{job_id}_segments.json` fallback в `TranscriptionService`
- [x] Навигация — кнопки в header — перемещены кнопки «Новое задание» и «Настройки» в `.app-controls`
- [x] Страница настроек — промпты отчётов — API endpoints + UI + навигация + fix автоподстановки
- [x] Дизайн-спецификация бейджа статуса отчёта — статус генерации отчёта в UI
- [x] Бейдж статуса генерации отчёта — backend tracking (`generating_reports` Set), endpoint `GET /report-status/{job_id}`, frontend polling, CSS пульсация

## Очередь

- [ ] Добавить индикатор прогресса при загрузке и обработке
- [ ] Добавить поддержку пакетной обработки файлов
- [ ] Улучшить мобильную версию веб-интерфейса
- [ ] Глобальный поиск по сохранённым транскрипциям
- [ ] Добавить горячие клавиши для основных действий
- [ ] Реализовать экспорт результатов в PDF
- [ ] Поддержка SRT формата для субтитров
- [ ] Контейнеризация через Docker
- [ ] Миграция на async/await для всех операций ввода-вывода
